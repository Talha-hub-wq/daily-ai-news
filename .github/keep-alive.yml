name: Keep Alive Render App

on:
  schedule:
    - cron: "*/5 * * * *"  # every 5 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send request
        run: curl https://daily-ai-news-merl.onrender.com/health